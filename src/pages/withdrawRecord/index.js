import Toast from "lib/vanui/dist/toast/toast";

Page({
  data: {
  },
  async onLoad() {
    // const str = await Promise.resolve(sayHello());
    // wx.showToast({
    //   title: str
    // });
  }
});